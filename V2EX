#!name = V2EX 每日签到
#!desc = V2EX 每日签到领取铜币，卡片式通知（昵称/余额/最近3条流水）
#!author = Ming
#!icon = https://raw.githubusercontent.com/lige47/QuanX-icon-rule/main/icon/04ProxySoft/V2ex.png
#!system = iOS
#!loon_version = 3.5.0
#!tag = Check-in
#!type = normal

[Argument]
获取Cookie = switch,false,desc=打开后访问 V2EX 任意页面即可抓取 Cookie

[Script]
http-request ^https?://www\.v2ex\.com/.*$ script-path=https://raw.githubusercontent.com/xhujer/-/refs/heads/main/V2EX.js, requires-body=false, timeout=10, tag=V2EX抓包, argument=[{获取Cookie}], enable=true
cron "10 9 * * *" script-path=https://raw.githubusercontent.com/xhujer/-/refs/heads/main/V2EX.js, timeout=60, tag=V2EX签到, enable=true

[MitM]
hostname = %APPEND% www.v2ex.com
