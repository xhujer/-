/**
 * HDHive 自动签到 (v3.0 最终修复版)
 * 修复了脚本提前结束导致无响应的问题
 */

const $ = new Env("HDHive");

// 配置
const CFG = {
    apiCheckin: 'https://hdhive.com/api/customer/user/checkin',
    watchApiPrefix: '/api/', 
};

const KEY_TOKEN = 'hdhive_token';
const KEY_CSRF = 'hdhive_csrf';

(async () => {
    if (typeof $request !== "undefined") {
        await captureToken();
    } else {
        await checkIn();
    }
})().catch((e) => $.logErr(e)).finally(() => $.done());

// --- 抓取逻辑 ---
async function captureToken() {
    const url = $request.url;
    if (url.indexOf("user/checkin") > -1) return;

    const h = $request.headers;
    const auth = h['Authorization'] || h['authorization'];
    const csrf = h['x-csrf-token'] || h['X-Csrf-Token'];

    if (auth && csrf) {
        // 去掉 Bearer 前缀存储
        const rawToken = auth.replace(/^Bearer\s+/i, '').trim();
        const rawCsrf = csrf.trim();
        const storedToken = $.getdata(KEY_TOKEN);
        
        if (rawToken !== storedToken) {
            $.setdata(rawToken, KEY_TOKEN);
            $.setdata(rawCsrf, KEY_CSRF);
            console.log(`[HDHive] 捕获新Token: ${rawToken.substring(0, 10)}...`);
            $.msg("HDHive", "🎉 抓取成功", "凭证已更新");
        }
    }
}

// --- 签到逻辑 (增加 Promise 等待) ---
async function checkIn() {
    const token = $.getdata(KEY_TOKEN);
    const csrf = $.getdata(KEY_CSRF);

    if (!token || !csrf) {
        $.msg("HDHive", "❌ 签到失败", "无凭证，请先用浏览器访问 hdhive.com");
        return;
    }

    // 严格复刻 UserScript 请求构造
    const finalToken = `Bearer ${token}`;
    const req = {
        url: CFG.apiCheckin,
        method: "POST",
        headers: {
            "accept": "application/json, text/plain, */*",
            "authorization": finalToken,
            "x-csrf-token": csrf,
            "origin": "https://hdhive.com",
            "referer": "https://hdhive.com/user/dashboard"
        },
        body: "" // 空字符串 Body
    };

    // [关键修复] 必须返回 Promise，让 await checkIn() 等待请求完成
    return new Promise((resolve) => {
        $.post(req, (err, resp, data) => {
            if (err) {
                $.msg("HDHive", "❌ 网络错误", "无法连接");
                console.log(err);
                resolve(); // 结束等待
                return;
            }

            console.log(`[HDHive] 状态码: ${resp.status}`);
            
            let isJson = false;
            let resJson = {};
            try {
                resJson = JSON.parse(data);
                isJson = true;
            } catch (e) {}

            if (resp.status === 200 || resp.status === 201) {
                const msg = (isJson && resJson.message) ? resJson.message : "签到成功";
                $.msg("HDHive", "✅ 签到成功", msg);
                console.log(`[HDHive] 响应: ${data}`);
            } else {
                if (resp.status === 401 || resp.status === 403) {
                    $.msg("HDHive", "⚠️ 凭证失效", "请重新登录网站");
                } else if (resp.status === 400) {
                     // 400 可能是"已签到"
                    const msg = (isJson && resJson.message) ? resJson.message : "请求被拒绝(400)";
                    $.msg("HDHive", "⚠️ 签到结果", msg);
                    console.log(`[HDHive] 400详情: ${data}`);
                } else {
                    $.msg("HDHive", "❌ 签到失败", `状态码 ${resp.status}`);
                }
            }
            resolve(); // 结束等待
        });
    });
}

// --- Env ---
function Env(name) {
    return {
        name: name,
        getdata: (k) => typeof $persistentStore !== "undefined" ? $persistentStore.read(k) : null,
        setdata: (v, k) => typeof $persistentStore !== "undefined" ? $persistentStore.write(v, k) : null,
        msg: (t, s, b) => {
            if (typeof $notification !== "undefined") $notification.post(t, s, b);
            console.log(`===${t}===\n${s}\n${b}`);
        },
        post: (o, c) => typeof $httpClient !== "undefined" ? $httpClient.post(o, c) : null,
        logErr: (e) => console.log(`❗️${name} Error: ${e}`),
        done: () => typeof $done !== "undefined" ? $done({}) : null
    };
}
