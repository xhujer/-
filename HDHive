#!name=HDHive 自动签到
#!desc=自动抓取Cookie，并每日自动签到、自动拉取最新积分流水。
#!author=ming
#!icon=https://hdhive.com/favicon.ico
#!homepage=https://hdhive.com/

[Argument]
# 默认每天早上9点签到
RunTime = input,"0 9 * * *",tag=每日签到时间,desc=Cron表达式
# 抓取开关，平时关掉省电，需要更新Cookie时打开
CaptureCookie = switch,true,tag=[开关] 自动获取Cookie,desc=开启后访问网页自动抓取，建议抓取成功后关闭

[Script]
# 每日定时签到 (远程脚本)
cron {RunTime} script-path=https://raw.githubusercontent.com/xhujer/-/refs/heads/main/HDHive%20Script, tag=HDHive每日签到, timeout=60

# 抓取 Cookie (远程脚本，排除 api 防止循环)
http-request ^https?:\/\/(?:www\.)?hdhive\.com\/(?!go-api|api)(?:$|user\b|dashboard\b) script-path=https://raw.githubusercontent.com/xhujer/-/refs/heads/main/HDHive%20Script, tag=HDHive获取Cookie, timeout=10, requires-body=false, enable={CaptureCookie}

[Mitm]
hostname = hdhive.com
